import pygsheets
from datetime import datetime

# gc = pygsheets.authorize(service_file='client_secret.json', no_cache=True)

def add_to_spreadsheet(prayer_content):
    # sheet = gc.open("Praylink").sheet1
    # new_row = [prayer_content, str(datetime.utcnow())]
    # index = len(sheet.get_all_records()) + 1
    # sheet.insert_rows(index, 1, new_row)
    return
